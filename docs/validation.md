# 验证记录

## 代码块与主题样式修复

- 将投机解码卡片的全局 `.token` 改为 `.draft-token`，避免污染 Prism 语法高亮。
- 显式配置 GitHub 浅色代码主题与 Dracula 深色代码主题，提高站点主题变量的选择器优先级。
- 浏览器检查 RLHF Python、τ-bench 纯文本流程的浅色和深色显示：高亮 token 背景透明、padding 和边框为 0；代码块背景随主题切换。
- 投机解码验证按钮与 8 张卡片显示正常，卡片仍保留 1px 边框。类型检查、生产构建通过。

## 2026-09-13：内容深化、代码与 Benchmark

- 15 篇主题章节与 Transformer 导读补充机制、推导、代码和答案，清理对话过程与名称猜测。
- 新增 Benchmark 目录与原始 τ-bench 详解，6 条任务释义链接到固定提交的官方任务数据。
- 新增 pass@k / pass^k 章节内实验；正文区分至少一次成功、全部成功、单题估计和跨题宏平均。
- `npm run typecheck`、10 项数值测试、17 个 Python 脚本、生产构建及断链检查通过。
- 静态输出检查：17 章含 KaTeX 渲染，无 `katex-error`；17 个 Python 附件地址对应实际构建文件。
- 修复 Docusaurus 对 `.py` 附件附加路由尾斜杠的问题，使用原生下载链接。
- 浏览器确认目录与详情导航正常；默认 `n=10,c=7,k=3` 显示 99.17% / 29.17%。切换 k=1 显示 70% / 70%，c=0 显示 0% / 0%，c=10 显示 100% / 100%。手机上 k=4 显示 100% / 16.67%。
- 目视检查桌面 Benchmark 目录、Transformer、手机指标实验、暗色任务表格与 DPO 长公式。390×844 手机尺寸下可用宽度375，页面无横向溢出；任务表和长公式在各自容器内滚动。
- 自测答案可展开；直接刷新章节正常；检查期间未捕获 error 级浏览器日志。
- 教学示例使用固定小策略、矩阵或轨迹；未训练真实 LLM、未运行模型横评、未测量真实推理性能。未推送或部署远端。

以下为此前阶段的历史记录。

## 2026-09-13：章节内实验与论文目录更新

- 保留现有文章结构；移除独立 `/lab/` 页面和导航入口，生产构建不再输出 `build/lab/index.html`。
- 新增 `/papers/` 论文目录和 Transformer 导读，目录、导读、返回链接在浏览器验证正常。
- DPO、GRPO、投机解码的章节内实验分别完成滑条、同分边界和草稿验证/提交操作。
- 论文目录已目视检查桌面与手机布局；手机可用内容宽度 375px，无页面横向溢出。
- 导读中的 Python 示例实际运行，输出与注释一致：`[0.67, 0.33]`、`[6.698, 6.605]`。
- 类型检查、6 项数值测试、静态构建及断链检查通过。

以下为初版历史记录，其中独立实验室页面已在本次更新中移除。

日期：2026-09-13。环境：macOS、Node.js 24.14.0、npm 11.9.0；锁定安装 Docusaurus 3.10.2、React 19.3.0、TypeScript 5.9.3。

## 自动检查

- `npm run typecheck`：通过。
- `npm test`：6 项通过。覆盖 DPO 中性偏好、损失单调性、数值导数一致性，以及 GRPO 二元奖励、零方差和仿射不变性。
- `npm run build`：静态生成通过，内部断链检查通过。
- `git diff --check`：通过。

## 真实浏览器检查

使用 Codex 内置浏览器访问本地生产静态预览，路径包含 `/frontier-algo-book/`：

- 首页布局、学习路线入口和实验室导航正常。
- DPO 滑条键盘操作到 Δ = -4 后，β = 0.5，页面显示偏好概率 11.9%、损失 2.127、导数 -0.440。
- GRPO 点击“全部同分”，四条优势均为 0.00，标准差为 0.00，显示“没有区分”。
- 投机解码第 4 位错误：前三位接受，第四位拒绝；提交后得到“今天 我们 学习 投机”。
- 投机解码全部正确：接受四个草稿并补一个目标 token；完成按钮禁用。
- DPO 章节 URL 直接访问并刷新正常，MDX 内嵌实验渲染正常。
- 手机尺寸 390 × 844（浏览器可用内容宽度 375）：章节、实验室和首页无页面横向溢出；章节菜单可展开；浅色与暗色可用。
- 已目视检查首页、移动端章节和暗色实验室截图。
- 实验室检查期间未捕获浏览器 error 级日志。

## 验证边界

尚未推送分支、合并或执行远端 GitHub Actions；Pages 在线地址未验证。UI 为教学示例，未运行真实模型或训练。外部论文链接经过调研核验，但未对所有外链执行自动可用性检测。

Docusaurus 构建末尾可能提示更新检查无法写入用户配置目录；这不影响编译与静态输出。Node.js 测试可能提示 TypeScript 模块自动识别警告，不影响测试结果。
