# 首版验证记录

日期：2026-09-13。环境：macOS、Node.js 24.14.0、npm 11.9.0；锁定安装 Docusaurus 3.10.2、React 19.3.0、TypeScript 5.9.3。

## 自动检查

- `npm run typecheck`：通过。
- `npm test`：6 项通过。覆盖 DPO 中性偏好、损失单调性、数值导数一致性，以及 GRPO 二元奖励、零方差和仿射不变性。
- `npm run build`：静态生成通过，内部断链检查通过。
- `git diff --check`：通过。

## 真实浏览器检查

使用 Codex 内置浏览器访问本地生产静态预览，路径包含 `/frontier-algo-book/`：

- 首页布局、学习路线入口和实验室导航正常。
- DPO 滑条键盘操作到 Δ = -4 后，β = 0.5，页面显示偏好概率 11.9%、损失 2.127、导数 -0.440。
- GRPO 点击“全部同分”，四条优势均为 0.00，标准差为 0.00，显示“没有区分”。
- 投机解码第 4 位错误：前三位接受，第四位拒绝；提交后得到“今天 我们 学习 投机”。
- 投机解码全部正确：接受四个草稿并补一个目标 token；完成按钮禁用。
- DPO 章节 URL 直接访问并刷新正常，MDX 内嵌实验渲染正常。
- 手机尺寸 390 × 844（浏览器可用内容宽度 375）：章节、实验室和首页无页面横向溢出；章节菜单可展开；浅色与暗色可用。
- 已目视检查首页、移动端章节和暗色实验室截图。
- 实验室检查期间未捕获浏览器 error 级日志。

## 验证边界

尚未推送分支、合并或执行远端 GitHub Actions；Pages 在线地址未验证。UI 为教学示例，未运行真实模型或训练。外部论文链接经过调研核验，但未对所有外链执行自动可用性检测。

Docusaurus 构建末尾可能提示更新检查无法写入用户配置目录；这不影响编译与静态输出。Node.js 测试可能提示 TypeScript 模块自动识别警告，不影响测试结果。
